# Media Mentions Tracker

This repository tracks public media mentions of selected companies.

It runs automatically every two hours using GitHub Actions and stores results as JSON files in:

```txt
data/companies/
